export function shortVibrate(times: number = 1): void {
  // Current HBuilderX 5.07 uni-app x compiler does not expose uni vibration APIs.
  // Keep this as a no-op so callers do not need platform guards.
  if (times < 1) return
}

export function longVibrate(): void {
  // See shortVibrate.
}
