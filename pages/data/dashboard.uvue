<template>
  <view class="page">
    <view class="bg-glow glow-purple"></view>
    <view class="bg-glow glow-blue"></view>
    <view class="bg-glow glow-pink"></view>
    <scroll-view class="scroll-area" scroll-y="true">
      <view class="dashboard-hero">
        <view class="dashboard-hero-copy">
          <text class="dashboard-kicker">微憩星球</text>
          <text class="dashboard-title">健康轨迹</text>
          <text class="dashboard-subtitle">每一次短暂停顿，都会在小星球上留下一点光。</text>
        </view>
        <view class="dashboard-planet">
          <view class="dashboard-orbit"></view>
          <text class="dashboard-planet-text">✦</text>
        </view>
      </view>

      <view class="section">
        <view class="section-header">
          <text class="section-title">今日概览</text>
        </view>
        <view class="section-body">
          <StatusIndicator title="护眼" :percent="eyeScore" :status="eyeStatus" />
          <StatusIndicator title="体态" :percent="postureScore" :status="postureStatus" />
          <StatusIndicator title="活力" :percent="vitalityScore" :status="vitalityStatus" />

          <view class="guard-box">
            <text class="guard-label">今日完成</text>
            <text class="guard-value">{{ guardCount }} 次</text>
            <text class="guard-sub">习惯渗透度 {{ penetrationText }}</text>
          </view>

          <view class="chart-container">
            <text class="chart-label">今天哪些时刻点亮了星球</text>
            <BarChart
              :labels="hourlyLabels"
              :values="hourlyValues"
              :height="220"
              barColor="#CBB8FF"
            />
          </view>

          <view class="list-section" v-if="detailCount > 0">
            <text class="chart-label">今日完成记录</text>
            <view class="detail-item" v-for="(item, idx) in todayDetails" :key="idx">
              <view class="detail-left">
                <text class="detail-name">{{ item.name }}</text>
                <text class="detail-type">{{ formatDetailType(item.type) }}</text>
              </view>
              <view class="detail-right">
                <text class="detail-duration">{{ item.durationSec }}秒</text>
                <text class="detail-time">{{ item.time }}</text>
              </view>
            </view>
          </view>
          <view class="empty-hint" v-if="detailCount < 1">
            <text class="empty-text">完成更多微动作后，这里会出现你的健康轨迹</text>
          </view>
        </view>
      </view>

      <view class="section">
        <view class="section-header">
          <text class="section-title">三颗星球趋势</text>
        </view>
        <view class="section-body">
          <view class="chart-container">
            <LineChart
              :series-values="weekTrendValues"
              :series-names="weekTrendNames"
              :series-colors="weekTrendColors"
              :point-labels="weekTrendLabels"
              :height="360"
              title="本周完成次数趋势（左右可拖动）"
            />
          </view>
        </view>
      </view>

      <view class="section">
        <view class="section-header">
          <text class="section-title">星球点亮记录</text>
        </view>
        <view class="section-body">
          <view class="chart-container">
            <HeatmapCalendar
              :dates="heatmapDates"
              :counts="heatmapCounts"
              :height="320"
            />
          </view>
          <view class="heatmap-legend">
            <text class="legend-text">点击格子查看当天详情</text>
          </view>
        </view>
      </view>

      <view class="section">
        <view class="section-header" @tap="toggleWeekly">
          <text class="section-title">本周星球报告</text>
          <text class="section-toggle">{{ weeklyToggleText }}</text>
        </view>
        <view class="section-body" v-if="weeklyShow">
          <view class="loading-box" v-if="weeklyReportLoading">
            <text class="loading-text">生成中</text>
          </view>
          <view class="report-box" v-if="weeklyLoaded && !weeklyReportLoading">
            <view class="report-stats">
              <view class="stat-item">
                <text class="stat-val">{{ wkTotalCompleted }}</text>
                <text class="stat-label">次完成</text>
              </view>
              <view class="stat-item">
                <text class="stat-val">{{ wkAvgGuard }}</text>
                <text class="stat-label">日均守护</text>
              </view>
              <view class="stat-item">
                <text class="stat-val">{{ wkBestDay }}</text>
                <text class="stat-label">最佳日次</text>
              </view>
            </view>
            <text class="report-body">{{ wkBody }}</text>
          </view>
          <view class="empty-hint" v-if="!weeklyLoaded && !weeklyReportLoading">
            <text class="empty-text">点击下方按钮生成周报</text>
            <view class="refresh-btn" @tap="refreshWeek">
              <text class="refresh-text">生成周报</text>
            </view>
          </view>
        </view>
      </view>

      <view class="bottom-spacer" />
    </scroll-view>
  </view>
</template>

<script setup lang="uts">
import StatusIndicator from '../../components/StatusIndicator.uvue'
import BarChart from '../../components/BarChart.uvue'
import LineChart from '../../components/LineChart.uvue'
import HeatmapCalendar from '../../components/HeatmapCalendar.uvue'
import { useAppStore } from '../../stores/appStore'
import { getHourlyData, getWeeklyTrend, getTodayDetail, ActionDetail, LineSeries } from '../../services/ScoreCalculator'
import { today } from '../../utils/TimeUtils'
import { BarItem } from '../../database/ActionLogDao'
import { DailyCount } from '../../models/DailyCount'

const store = useAppStore()

const eyeScore = computed<number>(() : number => store.eyeScore.value)
const eyeStatus = computed<string>(() : string => store.eyeStatus.value)
const postureScore = computed<number>(() : number => store.postureScore.value)
const postureStatus = computed<string>(() : string => store.postureStatus.value)
const vitalityScore = computed<number>(() : number => store.vitalityScore.value)
const vitalityStatus = computed<string>(() : string => store.vitalityStatus.value)
const guardCount = computed<number>(() : number => store.guardCount.value)
function getPenetrationText(): string {
  const p = store.penetration.value
  return '' + Math.round(p * 10) / 10
}
const penetrationText = computed<string>(getPenetrationText)
const heatmapData = computed<DailyCount[]>(() => store.heatmapData.value)

const hourlyLabels = ref<string[]>([])
const hourlyValues = ref<number[]>([])

const weekTrendValues = ref<number[][]>([])
const weekTrendNames = ref<string[]>([])
const weekTrendColors = ref<string[]>([])
const weekTrendLabels = ref<string[]>([])

const heatmapDates = ref<string[]>([])
const heatmapCounts = ref<number[]>([])

const weeklyShow = ref<boolean>(false)
const weeklyToggleText = computed<string>(() : string => weeklyShow.value ? '▼' : '▶')

const weeklyLoaded = computed<boolean>(() : boolean => store.weeklyReport.value != null)
const weeklyReportLoading = computed<boolean>(() : boolean => store.weeklyReportLoading.value)

function getWkBody(): string {
  const r = store.weeklyReport.value
  return r != null ? r.bodyMarkdown : ''
}
function getWkTotalCompleted(): number {
  const r = store.weeklyReport.value
  return r != null ? r.stats.totalCompleted : 0
}
function getWkAvgGuard(): number {
  const r = store.weeklyReport.value
  return r != null ? r.stats.avgGuardMinutes : 0
}
function getWkBestDay(): number {
  const r = store.weeklyReport.value
  return r != null ? r.stats.bestDayCount : 0
}
const wkBody = computed<string>(getWkBody)
const wkTotalCompleted = computed<number>(getWkTotalCompleted)
const wkAvgGuard = computed<number>(getWkAvgGuard)
const wkBestDay = computed<number>(getWkBestDay)

const todayDetails = ref<ActionDetail[]>([])
const detailCount = computed<number>(() : number => todayDetails.value.length)

function formatDetailType(t: string): string {
  if (t === 'eye') return '护眼'
  if (t === 'posture') return '体态'
  if (t === 'vitality') return '活力'
  if (t === 'breath') return '呼吸'
  if (t === 'neck_shoulder') return '肩颈'
  if (t === 'core') return '核心'
  if (t === 'lower_body') return '下肢'
  return t
}

function loadData(): void {
  const date = today()
  const hourly = getHourlyData(date)
  const hLabels: string[] = []
  const hValues: number[] = []
  for (let i = 0; i < hourly.length; i++) {
    hLabels.push(hourly[i].label)
    hValues.push(hourly[i].value)
  }
  hourlyLabels.value = hLabels
  hourlyValues.value = hValues

  const trend = getWeeklyTrend()
  if (trend.length > 0) {
    const first = trend[0]
    if (first != null) {
      weekTrendNames.value = [first.name]
      weekTrendColors.value = [first.color]
      const v: number[] = []
      const labels: string[] = []
      for (let i = 0; i < first.points.length; i++) {
        labels.push(first.points[i].label)
        v.push(first.points[i].value)
      }
      weekTrendLabels.value = labels
      weekTrendValues.value = [v]
    } else {
      weekTrendNames.value = []
      weekTrendColors.value = []
      weekTrendLabels.value = []
      weekTrendValues.value = []
    }
  } else {
    weekTrendNames.value = []
    weekTrendColors.value = []
    weekTrendLabels.value = []
    weekTrendValues.value = []
  }

  const heatmap = store.heatmapData.value
  const dArr: string[] = []
  const cArr: number[] = []
  for (let i = 0; i < heatmap.length; i++) {
    dArr.push(heatmap[i].date)
    cArr.push(heatmap[i].count)
  }
  heatmapDates.value = dArr
  heatmapCounts.value = cArr

  todayDetails.value = getTodayDetail()
}

function toggleWeekly(): void {
  weeklyShow.value = !weeklyShow.value
}

function refreshWeek(): void {
  weeklyShow.value = true
  store.refreshWeeklyReport()
}

onPageShow(() => {
  store.refreshHomeData()
  loadData()
})
</script>

<style scoped>
.page {
  flex: 1;
  background-color: #162038;
  background-image: linear-gradient(180deg, #131832 0%, #283252 45%, #4F587D 100%);
  position: relative;
  overflow: hidden;
}
.scroll-area {
  flex: 1;
  position: relative;
  z-index: 1;
}
.bg-glow {
  position: absolute;
  border-radius: 999px;
  z-index: 0;
}
.glow-purple {
  width: 210px;
  height: 210px;
  left: -82px;
  top: -54px;
  background-color: #776B97;
  opacity: 0.5;
}
.glow-blue {
  width: 168px;
  height: 168px;
  right: -60px;
  top: 104px;
  background-color: #C2E0EE;
  opacity: 0.24;
}
.glow-pink {
  width: 178px;
  height: 178px;
  left: 42px;
  bottom: 88px;
  background-color: #C68DC0;
  opacity: 0.18;
}
.sky-star {
  position: absolute;
  width: 3px;
  height: 3px;
  border-radius: 2px;
  background-color: rgba(255, 255, 255, 0.74);
  z-index: 0;
}
.star-a { left: 36px; top: 88px; }
.star-b { left: 118px; top: 144px; width: 4px; height: 4px; border-radius: 2px; }
.star-c { right: 52px; top: 116px; }
.star-d { right: 26px; top: 310px; width: 5px; height: 5px; border-radius: 3px; }
.star-e { left: 54px; top: 520px; }
.star-f { right: 86px; top: 420px; width: 4px; height: 4px; border-radius: 2px; }
.star-g { left: 138px; top: 610px; }
.star-h { right: 36px; top: 720px; width: 4px; height: 4px; border-radius: 2px; }
.star-i { left: 76px; top: 214px; width: 2px; height: 2px; border-radius: 1px; }
.star-j { right: 148px; top: 276px; width: 3px; height: 3px; border-radius: 2px; }
.star-k { left: 178px; top: 392px; width: 2px; height: 2px; border-radius: 1px; }
.star-l { right: 64px; top: 560px; width: 3px; height: 3px; border-radius: 2px; }

.dashboard-hero,
.settings-hero,
.planet-note,
.section,
.settings-section,
.guard-box,
.chart-container,
.report-stats,
.tag-card,
.header-block,
.filter-row,
.empty-block {
  background-color: rgba(29, 25, 62, 0.82);
  background-image: linear-gradient(135deg, rgba(48, 42, 94, 0.92) 0%, rgba(84, 65, 124, 0.72) 56%, rgba(198, 141, 192, 0.18) 100%);
  border-width: 1px;
  border-color: rgba(219, 200, 237, 0.18);
  border-style: solid;
  box-shadow: 0 18px 42px rgba(5, 6, 20, 0.28);
}
.dashboard-hero,
.settings-hero { margin: 22px 14px 12px 14px; padding: 22px 18px; border-radius: 28px; flex-direction: row; align-items: center; }
.dashboard-hero-copy,
.settings-hero-copy { flex: 1; flex-direction: column; padding-right: 12px; }
.dashboard-kicker,
.settings-kicker,
.section-toggle,
.setting-value,
.rule-priority,
.rule-source,
.detail-duration,
.guard-value,
.stat-val { color: #DBC8ED; }
.dashboard-kicker,
.settings-kicker { font-size: 13px; font-weight: bold; margin-bottom: 8px; }
.dashboard-title,
.settings-title,
.section-title,
.setting-label,
.planet-note-title,
.detail-name,
.report-body,
.report-title,
.header-title,
.tag-label { color: #FFFFFF; }
.dashboard-title,
.settings-title { font-size: 28px; font-weight: bold; line-height: 34px; }
.dashboard-subtitle,
.settings-subtitle,
.planet-note-text,
.setting-hint,
.guard-label,
.guard-sub,
.chart-label,
.legend-text,
.empty-text,
.loading-text,
.detail-type,
.detail-time,
.stat-label,
.rule-cond,
.rule-time,
.header-desc,
.tag-pkg,
.tag-uses,
.empty-hint { color: rgba(255, 255, 255, 0.72); }
.dashboard-subtitle,
.settings-subtitle { font-size: 13px; line-height: 20px; margin-top: 8px; }
.dashboard-planet,
.settings-planet { position: relative; width: 86px; height: 86px; border-radius: 43px; background-color: rgba(219, 200, 237, 0.18); align-items: center; justify-content: center; }
.dashboard-orbit,
.settings-orbit { position: absolute; width: 96px; height: 34px; border-radius: 48px; border-width: 2px; border-color: rgba(219, 200, 237, 0.38); border-style: solid; transform: rotate(-20deg); }
.dashboard-planet-text,
.settings-planet-text { color: #DBC8ED; font-size: 34px; }
.planet-note { margin: 0 14px 12px 14px; padding: 16px 18px; border-radius: 24px; }
.planet-note-title { font-size: 16px; font-weight: bold; margin-bottom: 6px; }
.planet-note-text { font-size: 12px; line-height: 19px; }
.section,
.settings-section { margin: 12px 14px; border-radius: 24px; overflow: hidden; }
.section-header { flex-direction: row; justify-content: space-between; align-items: center; padding: 16px 18px 8px 18px; }
.section-title { font-size: 18px; font-weight: bold; padding: 0; }
.section-body { padding: 8px 0 14px 0; }
.guard-box { margin: 8px 16px 12px 16px; padding: 18px; border-radius: 22px; align-items: center; }
.guard-value { font-size: 28px; font-weight: bold; margin-top: 4px; }
.chart-container { margin: 10px 16px 0 16px; padding: 14px 0 6px 0; border-radius: 22px; }
.chart-label { padding-left: 14px; margin-bottom: 0; font-size: 13px; font-weight: bold; }
.list-section { padding: 0 16px; }
.detail-item { flex-direction: row; justify-content: space-between; align-items: center; padding: 10px 0; border-bottom-width: 1px; border-bottom-color: rgba(219, 200, 237, 0.16); border-bottom-style: solid; }
.detail-left,
.detail-right,
.setting-left { flex-direction: column; }
.detail-right { align-items: flex-end; }
.detail-name { font-size: 14px; }
.detail-type,
.detail-time { font-size: 11px; margin-top: 2px; }
.detail-duration { font-size: 14px; font-weight: bold; }
.heatmap-legend,
.empty-hint,
.loading-box { align-items: center; padding: 20px; }
.report-box { padding: 16px; }
.report-body { font-size: 14px; line-height: 22px; white-space: pre-wrap; margin-top: 8px; }
.report-stats { flex-direction: row; justify-content: space-around; margin: 10px 16px; padding: 12px; border-radius: 18px; }
.stat-item { align-items: center; }
.stat-val { font-size: 20px; font-weight: bold; }
.refresh-btn { margin-top: 12px; padding: 10px 24px; background-color: #DBC8ED; border-radius: 18px; }
.refresh-text { color: #161A33; font-size: 14px; font-weight: bold; }
.setting-item { flex-direction: row; justify-content: space-between; align-items: center; padding: 15px 18px; border-bottom-width: 1px; border-bottom-color: rgba(219, 200, 237, 0.16); border-bottom-style: solid; }
.setting-left { flex: 1; }
.setting-label { font-size: 15px; font-weight: bold; }
.setting-hint { font-size: 12px; line-height: 18px; margin-top: 4px; }
.setting-value { font-size: 14px; font-weight: bold; }
.empty-rules { align-items: center; padding: 24px 16px 28px 16px; }
.rule-card { padding: 14px 18px; border-bottom-width: 1px; border-bottom-color: rgba(219, 200, 237, 0.16); border-bottom-style: solid; }
.rule-row1,
.rule-row2 { flex-direction: row; align-items: center; margin-top: 4px; }
.rule-type,
.rule-trigger { font-size: 11px; border-radius: 12px; padding: 4px 8px; margin-right: 6px; }
.rule-type { color: #FFFFFF; background-color: rgba(219, 200, 237, 0.18); font-weight: bold; }
.rule-trigger { color: rgba(255, 255, 255, 0.72); background-color: rgba(194, 224, 238, 0.12); }
.rule-priority { font-size: 11px; margin-left: auto; }
.rule-source,
.rule-cond { font-size: 11px; }
.rule-source { margin-right: 8px; }
.rule-cond { flex: 1; }
.rule-row3 { flex-direction: row; justify-content: space-between; margin-top: 4px; }
.rule-time { font-size: 10px; }
.header-block { margin: 22px 14px 12px 14px; padding: 20px 18px; border-radius: 28px; flex-direction: column; }
.header-title { font-size: 22px; font-weight: bold; margin-bottom: 8px; }
.header-desc { font-size: 12px; line-height: 19px; }
.filter-row { flex-direction: row; flex-wrap: wrap; margin: 0 14px 12px 14px; padding: 12px 12px 4px 12px; border-radius: 22px; }
.filter-chip { padding: 6px 12px; background-color: rgba(255, 255, 255, 0.1); border-radius: 14px; margin-right: 6px; margin-bottom: 8px; }
.filter-chip-active { background-color: #DBC8ED; }
.filter-chip-text { font-size: 12px; color: #FFFFFF; }
.empty-block { margin: 12px 14px; padding: 50px 20px; border-radius: 24px; align-items: center; }
.tag-list { padding: 0 14px 18px 14px; }
.tag-card { border-radius: 22px; padding: 14px; margin-bottom: 10px; flex-direction: row; }
.tag-card-left { flex: 1; flex-direction: column; }
.tag-card-right { flex-direction: column; align-items: flex-end; margin-left: 10px; }
.tag-label { font-size: 14px; font-weight: bold; margin-bottom: 3px; }
.tag-pkg { font-size: 11px; margin-bottom: 7px; }
.tag-source-row { flex-direction: row; align-items: center; }
.tag-source { font-size: 10px; padding: 3px 7px; border-radius: 8px; margin-right: 6px; }
.tag-source-llm { background-color: rgba(194, 224, 238, 0.14); color: #C2E0EE; }
.tag-source-user { background-color: rgba(143, 240, 195, 0.12); color: #8FF0C3; }
.tag-source-pending { background-color: rgba(219, 200, 237, 0.14); color: #DBC8ED; }
.cat-picker { flex-direction: row; align-items: center; background-color: rgba(255, 255, 255, 0.1); padding: 5px 10px; border-radius: 12px; margin-bottom: 6px; }
.cat-picker-text { font-size: 12px; color: #FFFFFF; margin-right: 4px; }
.cat-picker-arrow,
.lock-text { color: rgba(255, 255, 255, 0.68); }
.lock-text-on { color: #DBC8ED; font-weight: bold; }
.bottom-spacer { height: 132px; }
.page-bg,
.page-bg-img { display: none; }
</style>
